<% require 'csv' %>
<%= CSV.generate(headers: true, :quote_char=>'"', :force_quotes => true,) do |csv|
  csv << %w{name program location scheduled}
  @network_events.each do |row|
    csv << [row.name,row.program.name,row.location.name,row.scheduled_at]
  end
end .html_safe %>